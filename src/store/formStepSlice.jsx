import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const formStepSlice = createSlice({
  name: second,
  initialState,
  reducers: {}
});

export const {} = formStepSlice.actions

export default formStepSlice.reducer